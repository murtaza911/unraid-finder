version: "3.8"
services:
  unraid-finder:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./test-files:/mnt/user/TestFiles
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
      - JWT_SECRET=dev-secret-change-in-production
